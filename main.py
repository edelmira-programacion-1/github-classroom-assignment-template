# Este archivo está listo para ejecutar el programa.
# No debes modificar este archivo.

import sys
from solucion import [NOMBRE_DE_LA_FUNCION]

def main():
    # Leer una única línea de entrada y dividirla en palabras
    # input = sys.stdin.read().strip().split()

    # Leer varias líneas de entrada
    # input = sys.stdin.read().strip().splitlines()

    # Entrada manual para pruebas locales
    # input = input("Ingresa los datos de entrada: ").strip()

if __name__ == "__main__":
    main()